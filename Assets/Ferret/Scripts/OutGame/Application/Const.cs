using UnityEngine;

namespace Ferret.OutGame
{
    public sealed class RankingConfig
    {
        public const int SHOW_MAX_RANK = 30;
        public static readonly Color SELF_BACKGROUND_COLOR = new Color(1.0f, 0.5f, 0.5f, 0.5f);
    }
}